# ═══════════════════════════════════════════════
# Parallax — ML/Fine-tuning Requirements (optional)
# Install with: pip install -r requirements-ml.txt
# Only needed for training custom models on your data
# ═══════════════════════════════════════════════

-r requirements.txt

# Fine-tuning & Training
datasets>=3.0.0
transformers>=4.45.0
torch>=2.4.0
peft>=0.13.0
trl>=0.11.0
